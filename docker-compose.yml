version: '3.8'

services:
  bot:
    build: .
    container_name: laes-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      # Map the express server port to the host (host:container)
      - "5001:5001"
